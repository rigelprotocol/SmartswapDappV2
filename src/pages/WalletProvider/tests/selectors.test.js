// import { selectWalletProviderDomain } from '../selectors';

describe('selectWalletProviderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
