import { Button } from '@chakra-ui/react';
import React from 'react';

const CloseBtn: React.FC = () => (

    <Button variant='brand' color="white">
        Prev
    </Button>

);

export default CloseBtn;
