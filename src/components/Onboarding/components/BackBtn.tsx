import { Button } from '@chakra-ui/react';
import React from 'react';

const BackBtn: React.FC = () => (

    <Button variant='brand' border='solid' borderColor='white' color="white">
        Prev
    </Button>

);

export default BackBtn;
